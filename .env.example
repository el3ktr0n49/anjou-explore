# ═══════════════════════════════════════════════════════════
# Anjou Explore - Environment Variables Template
# ═══════════════════════════════════════════════════════════
#
# Instructions:
# 1. Copier ce fichier vers .env
# 2. Remplacer les valeurs entre <...> par vos vraies valeurs
# 3. Ne JAMAIS committer le fichier .env (déjà dans .gitignore)
#
# ═══════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────
# DATABASE
# ───────────────────────────────────────────────────────────

# URL de connexion PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://anjou:anjou_password_2026@localhost:5432/anjouexplore"

# ───────────────────────────────────────────────────────────
# AUTHENTICATION
# ───────────────────────────────────────────────────────────

# NOTE: Les mots de passe des admins sont individuels et stockés en base de données.
# - Au premier seed: mots de passe par défaut (format: AnjouExplore2026_<Prénom>)
# - Changement obligatoire au premier login
# - Pour réinitialiser un mot de passe: bun run admin:reset-password <adminName>

# Secret pour signer les JWT tokens (min 32 caractères aléatoires)
# Générer avec: openssl rand -base64 32
JWT_SECRET="<générer_avec_openssl_rand_base64_32>"

# Durée de validité des sessions JWT (en heures)
JWT_EXPIRATION_HOURS=24

# Activer/désactiver l'authentification 2FA
# IMPORTANT: Doit TOUJOURS être "true" en production !
# En développement: "false" pour faciliter les tests
ENABLE_2FA="false"  # false en dev, TOUJOURS true en production

# ───────────────────────────────────────────────────────────
# EMAIL (Resend.com)
# ───────────────────────────────────────────────────────────

# Clé API Resend (gratuit jusqu'à 3000 emails/mois)
# Obtenir sur: https://resend.com/api-keys
RESEND_API_KEY="<votre_clé_resend_ici>"

# Email expéditeur (doit être un domaine vérifié sur Resend)
EMAIL_FROM="<no-reply@votre-domaine.com>"

# ───────────────────────────────────────────────────────────
# SUMUP (Payment Gateway)
# ───────────────────────────────────────────────────────────

# Clé API SumUp (obtenir sur: https://developer.sumup.com)
# IMPORTANT: Utiliser sup_sk_xxx (Secret Key), pas sup_pk_xxx (Public Key)
SUMUP_API_KEY="<votre_clé_sumup>"

# Email du marchand SumUp (requis pour créer des checkouts)
SUMUP_PAY_TO_EMAIL="<votre_email_marchand>"

# Code marchand SumUp
SUMUP_MERCHANT_CODE="<votre_code_marchand>"

# ───────────────────────────────────────────────────────────
# APPLICATION
# ───────────────────────────────────────────────────────────

# URL de base de l'application (pour emails, redirections)
# Development: http://localhost:4321
# Production: https://www.anjouexplore.com
APP_URL="http://localhost:4321"

# Environnement
NODE_ENV="development"  # ou "production"

# ───────────────────────────────────────────────────────────
# SÉCURITÉ
# ───────────────────────────────────────────────────────────

# CORS Origins autorisés (séparés par des virgules)
CORS_ORIGINS="http://localhost:4321,http://localhost:3000"

# Cookie Secure (true en production uniquement)
COOKIE_SECURE="false"  # true en production avec HTTPS

# ───────────────────────────────────────────────────────────
# OPTIONNEL: LOGGING & MONITORING
# ───────────────────────────────────────────────────────────

# Niveau de log (debug, info, warn, error)
LOG_LEVEL="info"

# Activer les logs de requêtes Prisma
# DEBUG="prisma:query"
