# ⚠️ NE PAS COMMITER CE FICHIER AVEC DE VRAIES VALEURS
# Copier vers postgres-secret.yaml et remplacer les valeurs
# Puis appliquer avec : kubectl apply -f k8s/postgres-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: anjouexplore
type: Opaque
stringData:
  POSTGRES_USER: "anjouexplore"
  POSTGRES_PASSWORD: "CHANGEME_STRONG_PASSWORD"  # ⚠️ Générer un mot de passe fort
  POSTGRES_DB: "anjouexplore"
  # DATABASE_URL utilisée par Prisma
  DATABASE_URL: "postgresql://anjouexplore:CHANGEME_STRONG_PASSWORD@postgres:5432/anjouexplore?schema=public"
