# ⚠️ NE PAS COMMITER CE FICHIER AVEC DE VRAIES VALEURS
# Copier vers backup-s3-secret.yaml et remplacer les valeurs
# Puis appliquer avec : kubectl apply -f k8s/backup-s3-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: s3-backup-secret
  namespace: anjouexplore
type: Opaque
stringData:
  # AWS S3 / Scaleway Object Storage
  S3_ENDPOINT: "https://s3.fr-par.scw.cloud"  # Scaleway Paris ou AWS endpoint
  S3_BUCKET: "anjouexplore-backups"
  S3_ACCESS_KEY_ID: "CHANGEME"
  S3_SECRET_ACCESS_KEY: "CHANGEME"
  S3_REGION: "fr-par"  # Région Scaleway ou AWS
