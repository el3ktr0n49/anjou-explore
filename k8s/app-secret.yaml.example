# ⚠️ NE PAS COMMITER CE FICHIER AVEC DE VRAIES VALEURS
# Copier vers app-secret.yaml et remplacer les valeurs
# Puis appliquer avec : kubectl apply -f k8s/app-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: anjouexplore-secret
  namespace: anjouexplore
type: Opaque
stringData:
  # Database (référence le même que postgres-secret)
  DATABASE_URL: "postgresql://anjouexplore:CHANGEME_STRONG_PASSWORD@postgres:5432/anjouexplore?schema=public"

  # Auth & Security
  # Note : Les mots de passe admins sont stockés individuellement en BDD (table Admin)
  # Ils sont configurés lors du seed initial (bun run db:seed)
  JWT_SECRET: "CHANGEME_64_RANDOM_CHARS_MIN"  # Minimum 32 caractères

  # Email (Resend)
  RESEND_API_KEY: "re_CHANGEME"

  # Payment (SumUp)
  SUMUP_API_KEY: "sup_sk_CHANGEME"
  SUMUP_MERCHANT_CODE: "CHANGEME"
  SUMUP_PAY_TO_EMAIL: "CHANGEME@gmail.com"
