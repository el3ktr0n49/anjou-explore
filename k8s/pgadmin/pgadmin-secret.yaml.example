# ⚠️ NE PAS COMMITER CE FICHIER AVEC DE VRAIES VALEURS
# Copier vers pgadmin-secret.yaml et remplacer les valeurs
# Puis appliquer avec : kubectl apply -f k8s/pgadmin/pgadmin-secret.yaml

apiVersion: v1
kind: Secret
metadata:
  name: pgadmin-secret
  namespace: anjouexplore
type: Opaque
stringData:
  PGADMIN_DEFAULT_EMAIL: "CHANGEME@CHANGEME.com"
  PGADMIN_DEFAULT_PASSWORD: "CHANGEME_STRONG_PASSWORD"  # Mot de passe pgAdmin
